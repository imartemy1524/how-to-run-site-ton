export default function Second() {
    return (
        <>
            <h2>Step #2</h2>
            <p>
                Next, you run your website on some network, if you don't want to have access to it from usual the
                internet, you can run it on localhost.
            </p>
            <p>
                For example, this app is running on <code>localhost:3000</code> using <code>npx http-server -p 3000</code> command.
            </p>
        </>
    )
}